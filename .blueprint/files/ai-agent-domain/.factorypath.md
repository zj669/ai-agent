# Blueprint: `ai-agent-domain/.factorypath`

## Metadata
- file: `ai-agent-domain/.factorypath`
- version: `v0.1.0`
- status: 正常
- updated_at: 2026-02-15 07:36
- owner: backend-blueprint-shadow

### 状态机
`正常 -> 待修改 -> 修改中 -> 修改完成 -> 正常`
允许回退:
`修改中 -> 待修改`
`修改完成 -> 修改中`

## 1) 整体文件职责
- 做什么:
  - 记录工程元配置，确保 IDE/构建工具行为一致。
- 不做什么:
  - 不直接承担跨模块编排职责（由上层服务或调用方负责）。

## 2) 核心方法
| 方法名 | 角色 | 上游调用方 | 下游依赖 | 备注 |
|---|---|---|---|---|
| `tag <factorypath>` | 文件内关键符号/配置项 | 对应模块调用链 | 项目内类型或外部工具链 | 自动回填 |
| `tag <factorypathentry>` | 文件内关键符号/配置项 | 对应模块调用链 | 项目内类型或外部工具链 | 自动回填 |

## 3) 具体方法
### `tag <factorypath>`
- Signature: `tag <factorypath>`
- 入参:
  - 由声明式文件或调用方上下文提供。
- 出参:
  - 产生配置结果、类型定义或运行时行为约束。
- 功能含义:
  - 在 `ai-agent-domain/.factorypath` 中承担核心定义职责。
- 链路作用:
  - 上游: 由模块内服务、框架装配或构建流程触发。
  - 下游: 影响业务执行、配置解析或运行时集成。
- 副作用/外部依赖:
  - 同目录配置文件与构建/运行工具链

### `tag <factorypathentry>`
- Signature: `tag <factorypathentry>`
- 入参:
  - 由声明式文件或调用方上下文提供。
- 出参:
  - 产生配置结果、类型定义或运行时行为约束。
- 功能含义:
  - 在 `ai-agent-domain/.factorypath` 中承担核心定义职责。
- 链路作用:
  - 上游: 由模块内服务、框架装配或构建流程触发。
  - 下游: 影响业务执行、配置解析或运行时集成。
- 副作用/外部依赖:
  - 同目录配置文件与构建/运行工具链

## 4) 变更记录（每次变更一段话）
- [2026-02-15 07:36] [v0.1.1] [状态: 正常 -> 正常]
  - 自动回填占位蓝图为源码职责说明，移除模板占位字段。

## 5) Temp缓存区（仅 status=待修改 时保留）
- 当前状态为正常，缓存区留空。
