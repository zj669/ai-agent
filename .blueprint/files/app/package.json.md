## Metadata
- file: `.blueprint/files/app/package.json.md`
- version: `1.0`
- status: 正常
- updated_at: 2026-02-16
- owner: blueprint-team

## 状态机
- 状态集合: `正常` / `待修改` / `修改中` / `修改完成`
- 允许流转: `正常 -> 待修改 -> 修改中 -> 修改完成 -> 正常`
- 允许回退: `修改中 -> 待修改`、`修改完成 -> 修改中`

## 1) 整体文件职责
- 主题: app/package.json
- 该文件用于声明 `app/` 子应用脚本入口。当前职责为将默认 `dev` 指向主前端 `ai-agent-foward`，避免误启动 demo 页面。

## 2) 核心方法
- 当前文件以脚本配置说明为主，无明确可执行方法。

## 3) 具体方法
### 3.1 文档型蓝图说明
- 函数签名: `N/A`
- 入参: 无
- 出参: 无
- 功能含义: 约束 `app/` 目录下 npm 脚本的默认行为与主前端映射。
- 链路作用: 开发者在 `app/` 下执行 `npm run dev` 时，转发到主前端运行。

## 4) 变更记录
- 2026-02-16: 新增蓝图。默认 `dev` 重定向到 `../ai-agent-foward`，用于在 `app/` 目录执行 `npm run dev` 时启动主前端。

## 5) Temp缓存区
当前状态为 `正常`，本区留空。
