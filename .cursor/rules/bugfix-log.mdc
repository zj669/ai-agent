---
description: 遇到 bug、报错或异常行为时，先查阅 docs/bugfix-log/ 目录获取历史问题和解决方案上下文；修复完成后在该目录中记录本次问题和解法。
globs:
alwaysApply: true
---

# Bug 修复记录规范

## 遇到问题时（必须执行）

在分析任何 bug、报错、编译失败、运行异常或用户反馈的问题之前，**必须先读取** `docs/bugfix-log/README.md`，了解历史问题列表，并按需读取相关的具体记录文件，以获取：

- 是否存在相同或相似的历史问题
- 之前采用的修复方案和根因分析
- 涉及的文件和改动要点

如果历史记录中有匹配的问题，优先参考已有方案，避免重复踩坑。

## 修复完成后（必须执行）

每次修复 bug 后，**必须在 `docs/bugfix-log/` 目录下新建一个记录文件**，命名格式：

```
YYYY-MM-DD-<模块简称>-<问题简述（用连字符，不超过5个词）>.md
```

示例：
- `2026-03-06-swarm-chat-ux-bugs.md`
- `2026-03-07-workflow-execution-timeout.md`

**同时更新 `docs/bugfix-log/README.md`** 中的记录列表表格，添加新条目。

## 记录文件格式

```markdown
# Bug 记录：<问题标题>

| 项目 | 内容 |
|------|------|
| **日期** | YYYY-MM-DD |
| **模块** | 前端/后端 + 具体模块名 |
| **严重度** | P0（阻塞）/ P1（影响体验）/ P2（轻微） |
| **发现方式** | 用户反馈 / 编译报错 / 测试发现 |

---

## 问题描述

<具体现象，包括错误信息、复现步骤>

**根因**：<问题的根本原因>

---

## 修复方案

<修复思路和关键代码片段>

---

## 涉及文件

| 文件 | 操作 | 改动要点 |
|------|------|----------|
| `路径/文件名` | 新建/修改/删除 | 关键改动说明 |

---

## 验证

<验证修复有效的方式，如编译命令输出、测试结果等>
```

## 重要原则

- 记录要**具体可复现**，包含足够的错误信息和上下文
- 修复方案要**包含关键代码**，让后来者能理解改动意图
- 如果修复涉及多个相关问题，可以合并到一个文件中记录
- 已记录的历史问题**不要删除**，可以追加"后续更新"小节
