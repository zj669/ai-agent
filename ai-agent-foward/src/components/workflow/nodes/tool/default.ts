import type { ToolNodeData } from './types';

export const defaultToolNodeData: ToolNodeData = {
  label: 'Tool',
  nodeType: 'tool',
  config: {
    toolName: 'http_request',
    inputTemplate: ''
  }
};
