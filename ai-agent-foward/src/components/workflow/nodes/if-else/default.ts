import type { IfElseNodeData } from './types';

export const defaultIfElseNodeData: IfElseNodeData = {
  label: 'If / Else',
  nodeType: 'if-else',
  config: {
    mode: 'expression',
    expression: ''
  }
};
